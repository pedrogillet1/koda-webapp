/**
 * NER Service - STUB (service removed)
 */

export const extractEntities = async () => [];
export const analyzeText = async () => ({ entities: [], keywords: [] });

export default {
  extractEntities,
  analyzeText,
};
