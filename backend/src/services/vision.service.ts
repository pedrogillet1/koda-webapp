/**
 * Vision Service - STUB (service removed)
 */

export const analyzeImage = async () => ({ description: '', labels: [], text: '' });
export const extractTextFromImage = async () => '';

export default {
  analyzeImage,
  extractTextFromImage,
};
