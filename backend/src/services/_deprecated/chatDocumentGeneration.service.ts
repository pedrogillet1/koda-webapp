/**
 * Chat Document Generation Service - STUB (service removed)
 * This stub file prevents import errors while the service is removed.
 */

export const generateDocument = async (params: any) => {
  return {
    message: 'Document generation service removed',
    chatDocument: { id: 'stub' }
  };
};

export default {
  generateDocument,
};
